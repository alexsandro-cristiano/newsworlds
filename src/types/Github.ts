export interface IGithubData {
  name: string;
  avatar_url: string;
  html_url: string;
  blog?: string;
  location: string;
  bio: string;
  public_repos: number;
}
export interface IGithub {
  user: string;
}
